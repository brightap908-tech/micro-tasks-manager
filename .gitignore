# Python
__pycache__/
*.py[cod]
*.pyo
.pythonlibs/
*.egg-info/
dist/
build/
*.egg

# Secrets & database
.encryption.key
microtask_manager.db
backups/

# Node
node_modules/
package-lock.json
frontend/package-lock.json
# frontend/dist is intentionally committed so Render (Python runtime)
# does not need Node.js during deployment — dist is pre-built and pushed.

# Env
.env
.env.local

# Editor
.vscode/
.idea/
*.swp

# Replit internal (do not ignore)
# .replit — intentionally kept
