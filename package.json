{
  "name": "microtask-manager",
  "version": "1.0.0",
  "description": "Private AI-powered micro tasks automation manager.",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brightap908-tech/micro-tasks-manager.git"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:playwright": "python -m playwright install --with-deps chromium",
    "build:full": "npm install && python -m playwright install --with-deps chromium && npm run build"
  },
  "author": "",
  "license": "ISC"
}
